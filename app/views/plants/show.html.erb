<div class="container">

  <div class="card-show">
    <img src=" <%=@plant.image%>" alt="<%= @plant.name %>">
    <div class ="card-text rounded">
      <h1> <%= @plant.name %> </h1>
      <p> <%= @plant.description %> </p>
      <%= link_to 'Rent Plant', new_plant_booking_path(@plant), class: "btn btn-success" %>
    </div>
  </div>

</div>
